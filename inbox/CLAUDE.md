# inbox

Все новые заметки автоматически попадают сюда. Бот только добавляет файлы
в `inbox/`: он не перемещает, не переименовывает и не редактирует уже
существующие заметки. Разбор — вручную или Claude по процедуре ниже.

## Процедура разбора (полуавтоматическая)

1. Прочитай все заметки inbox (кроме CLAUDE.md).
2. Составь план на всю пачку и покажи его пользователю одной таблицей:
   `файл → назначение` (папка/подпапка или living-файл+лог), новый слаг,
   объединения заметок одного `capture_id` с пересекающимся содержанием
   (бот дробит одну голосовую на куски), `related:`-мосты. Спорные строки пометь
   знаком `?` с одним коротким вопросом.
   **Противоречия:** заметка расходится с шапкой living-файла или related-заметкой
   (другие числа, отменённое решение) → не перезаписывать молча, а показать в плане
   пометкой `⚡ противоречит [[файл]]: было X, стало Y` и спросить, что считать
   актуальным. Подтверждённое новое значение → обновить шапку/заметку-хозяина,
   старое остаётся в логе/заметке как история.
3. Дождись подтверждения. Пользователь правит только исключения — молчание по
   строке = согласие с планом.
4. Исполни весь план разом: событие для living-файла → строка в лог + шапка
   (см. «Living-файлы» в корневом CLAUDE.md); остальное — переименовать по
   конвенции, канонический frontmatter, `related:`, тело — по шаблону жанра
   из `templates/` (если жанр очевиден), переместить по таблице маршрутизации.
5. Запусти `python3 scripts/rebuild_indexes.py`.
6. Добавь строку `ingest` в корневой `LOG.md` (итог разбора одной фразой).
7. Inbox после разбора пуст (кроме CLAUDE.md).

## Маршрутизация

| Тема | Папка |
|---|---|
| Revolut: проекты, promotion, смены, зарплата | `work/` |
| CV, интервью, отклики, drills | `job-search/` |
| Инвестиции, крипта, депозиты, бюджет, налоги | `finance/` |
| Спорт, сон, питание, врачи | `health/` |
| Курсы, книги, конспекты | `learning/` |
| Активный личный проект | `projects/<имя>/` |
| Идея без проекта и без действий по ней | `ideas/` |

Спорные случаи:
- Инвестиционная стратегия → `finance/`, даже если упоминается работа или переезд.
- Идея нового проекта → `ideas/`, пока по ней нет реальных действий; начал делать → `projects/<имя>/`.
- Зарплата/смены на текущей работе → `work/`, финансовый расчёт из той же заметки → `finance/`
  (разбить на две + связать через `related:`).
- Не уверен → `ideas/` + пометка в «Заметки для еженедельного обзора» в корневом `index.md`.
- Есть подходящая тематическая подпапка → класть сразу в неё.

## Frontmatter (канонический формат)

```yaml
date: YYYY-MM-DD           # дата заметки
source: "[[captures/...]]" # ссылка на исходный capture (от бота)
capture_id: "tg-..."       # сохранять при разборе
project: finance           # папка назначения (с подпапкой, если есть)
tags: [investing, crypto]  # из словаря тегов ниже
status: processed          # raw | processed | archived
summary: "1–2 предложения"
related:                   # опционально: мосты к заметкам других папок
  - "[[work/2026-07-15-реквизишн-тимлид-и-ночные-смены]]"
origin: claude             # только если выводы синтезировал Claude, а не сказал пользователь
```

`origin: claude` — для заметок, чьё содержание сгенерировано Claude (аналитика,
сравнения, выводы сессий), а не произнесено/написано пользователем. Такие факты
не цитировать как утверждения пользователя и не опираться на них при обновлении
шапок living-файлов, пока пользователь их не подтвердил; подтвердил — поле убрать.
У заметок из голосовых `origin` не ставится.

Бот пишет свой формат (`created, capture_id, source, destination`) — при разборе
конвертировать в канонический.

Статусы: `raw` — как пришло от бота; `processed` — отсортировано, канонический
frontmatter; `archived` — неактуально, хранится для истории.

`summary` — самодостаточный: 1–2 предложения с конкретикой (числа, решения,
имена), чтобы по нему можно было принять решение о заметке, не открывая её.
Не «мысли про X».

Имена файлов: `YYYY-MM-DD-slug.md`. Слаг — по-русски, коротко, без времени
и tg-суффиксов.

## Словарь тегов

`promotion, team-lead, salary, revolut` (work) · `cv, interview, remote, applications, networking` (job-search) ·
`investing, crypto, portfolio, deposit, strategy, dca, ibkr, x-stocks` (finance) ·
`bot, wikibot, deploy, automation, freelance` (projects) ·
`экономика, конспект, курсы` (learning) ·
`инструменты, продуктивность, доход, бизнес-идея, telegram-бот, ai-agent, saas` (ideas) ·
`cashcat, arcus, расходы` (finance) · `uzbekistan` (сквозной).
Новый тег — добавь сюда же.
